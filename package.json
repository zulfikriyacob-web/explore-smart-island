{
  "name": "explore-smart-island",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "KSSR Year 1-3 practice wrapped as an island game. Phase 1 core logic.",
  "scripts": {
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "validate:content": "node scripts/validate-content.js"
  },
  "engines": {
    "node": ">=22.18.0"
  },
  "dependencies": {
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "@vitest/coverage-v8": "^5.0.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
